[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mass_spectrometry_foundation_model"
readme = "README.md"
license = { file = "LICENSE.md" }
version = "0.0.1"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src"]
